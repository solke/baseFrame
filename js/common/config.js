
import { NativeModules, Platform, StyleSheet, Dimensions,DeviceInfo} from 'react-native'
export const isIphoneX = DeviceInfo.isIPhoneX_deprecated
export const http = 'http://'
//存放请求接口
export const api = {
    //common

};